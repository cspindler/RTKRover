# Known boards (ESP32 Feathers of the RTK headtracker assemblies), by CP2104
# USB-serial serial number, mapped to the assembly label (= BLE name = sticker).
#
# The serial number is baked into the macOS device path
# (/dev/cu.usbserial-<SERIAL>), so listing boards here lets the tooling discover
# which board is attached instead of trusting a hardcoded port.
#
# Find the serial of a newly added board with:
#     pio device list
# and look for "SER=..." on the CP2104 entry.
#
# Format:  <SERIAL>  <assembly label>
# Lines starting with # and blank lines are ignored.

01562BEE rwa-hs-1
01562AF9 rwa-hs-2
01643EB8 rwa-hs-3
02860F06 rwa-hs-4
01562CE8 rwa-hs-5
